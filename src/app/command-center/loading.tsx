export default function LoadingCommandCenter() {
  return (
    <main className="psos-loading-shell">
      <div className="psos-loading-orb" />
      <div>
        <div className="text-[15px] tracking-[.2em] text-[var(--text-secondary)]">PHOTO STUDIO OS</div>
        <div className="mt-3 text-[32px] text-[var(--text-primary)]">Loading Command Center</div>
      </div>
    </main>
  );
}
